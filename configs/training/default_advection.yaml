# Data options
data_dir: advection
fourier_features: false
online: false
bootstrap: false

# Model/ensemble options
model_name: null
ensemble_size: 0
ensemble_name: null
model_type: ResOrthoONet

# Training hyperparameters
iterations: 40000
lr: 0.001
batch_size: null
loss: MSE

# Layer sizes
layers: [20, 20, 20, 20, 20, 20, 20]

# Scheduler options
decay_gamma: 0.9999653432 # Approximately 20,000 steps
minimum_lr: 0.0005

# Reproducibility and Debugging
verbose: false
display_every: 1000